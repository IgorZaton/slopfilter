## Cursor Cloud specific instructions

This is a blank repository with no application code, dependencies, or services. It contains only a `README.md`.

- **No build/test/lint commands** exist yet. When application code is added, update this section with the relevant commands.
- **No dependencies** to install. When a `package.json`, `requirements.txt`, or similar is added, the VM update script should be updated accordingly.
- **No services** to start. When services are added, document startup instructions here.
